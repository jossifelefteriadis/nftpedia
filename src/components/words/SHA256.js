import React from "react";

const SHA256 = () => (
  <section className="word-description-container">
    <h1>SHA-256</h1>
    <p>
      SHA-256 (Secure Hash Algorithm 256) är en algorytm som används för att
      konvertera en inmatning oavsett längd, till en sträng på förbestämd längd
      på 256 bits (32 bytes).
    </p>
  </section>
);

export default SHA256;
