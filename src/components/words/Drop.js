import React from "react";

const Drop = () => (
  <section className="word-description-container">
    <h1>Drop</h1>
    <p>
      Drop (NFT Drop) är refererar till ett datum då ett specifikt NFT
      kollektion lanseras och blir tillgängligt för alla att köpa.
    </p>
  </section>
);

export default Drop;
