import React from "react";

const SeedPhrase = () => (
  <section className="word-description-container">
    <h1>SEED PHRASE</h1>
    <p>
      Seed Phrase är en lista av ord (vanligtvis 12-24 stycken) som kan användas
      för att återfå tillgång till din kryptoplånbok.
    </p>
  </section>
);

export default SeedPhrase;
