import React from "react";

const IPFS = () => (
  <section className="word-description-container">
    <h1>IPFS</h1>
    <p>
      IPFS är distribuerat system som används för att lagra och komma åt filer,
      hemsidor, data, osv.
    </p>
  </section>
);

export default IPFS;
